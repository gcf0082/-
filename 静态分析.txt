请提取以下文本中的: 函数,入口函数，危险函数，不包含的函数和操作。
示例1：获取函数abcd的向上调用链  --函数是abcd，操作是向上调用链；
示例2：获取函数abcd的向下调用链，危险函数是exec  --函数是abcd，操作是向下调用链，危险函数是exec；
问题: 函数gcf的向下调用链，危险函数是exec，rmfile 但是不包含abc，入口函数是mytest，以json返回

{  
  "函数": "gcf",  
  "操作": "向下调用链",  
  "入口函数": "mytest",  
  "危险函数": ["exec", "rmfile"],  
  "不包含的函数": "abc"  
}```


请按如下例子提取信息，结果按json返回，如果失败不出来给出错误：
示例1：获取函数abcd的向上调用链  --结果：函数是abcd，操作是向上调用链；
示例1：获取方法abcd的向上调用链  --结果：函数是abcd，操作是向上调用链；
示例2：获取函数abcd的向下调用链，危险函数是exec  --结果：函数是abcd，操作是向下调用链，危险函数是exec；
示例2：获取方法abcd的向下调用链，危险方法是exec  --结果：函数是abcd，操作是向下调用链，危险函数是exec；
示例3：获取函数abcd的向下调用链，危险函数以xxx结尾  --结果：函数是abcd，操作是向下调用链，危险函数是*xxx；
示例3：获取函数abcd的向下调用链，危险函数包含xxx  --结果：函数是abcd，操作是向下调用链，危险函数是*xxx*；
示例4：获取函数abcd的向下调用链，只看包含污点传播的  --结果：函数是abcd，操作是向下调用链，污点传播为true；
示例3：获取类abcd的危险函数  --结果：操作是危险函数，类是abcd
示例5：有哪些项目  --结果：操作是查找项目，对象是所有
示例6：告诉我所有的项目  --结果：操作是查找项目，对象是所有
示例7：列出所有dme开头的项目 --结果：操作是列出项目，对象是dme*
示例8：列出所有abc结尾的项目 --结果：操作是列出项目，对象是*abc
示例9：列出所有包含abc的项目 --结果：操作是列出项目，对象是*abc*
示例：abc项目的所有危险函数  --结果：操作是列出危险函数，项目是abc
示例：有哪些危险函数  --结果：操作是列出危险函数

问题：有哪些危险函数
