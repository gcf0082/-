请提取以下文本中的: 函数,入口函数，危险函数，不包含的函数和操作。
示例1：获取函数abcd的向上调用链  --函数是abcd，操作是向上调用链；
示例2：获取函数abcd的向下调用链，危险函数是exec  --函数是abcd，操作是向下调用链，危险函数是exec；
问题: 函数gcf的向下调用链，危险函数是exec，rmfile 但是不包含abc，入口函数是mytest，以json返回

{  
  "函数": "gcf",  
  "操作": "向下调用链",  
  "入口函数": "mytest",  
  "危险函数": ["exec", "rmfile"],  
  "不包含的函数": "abc"  
}```
